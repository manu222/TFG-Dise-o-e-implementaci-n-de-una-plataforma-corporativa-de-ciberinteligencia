```mermaid
graph TD

    subgraph "INTERNET (Acceso Remoto)"

        UserPC[Ordenador Local Manuel]

        Termius[Termius SSH Client]

        UserPC --> Termius

    end

  

    subgraph "OVH DEDICATED SERVER"

        NIC[Interfaz Física Public IP]

        ProxmoxHost[Proxmox VE Host]

        subgraph "Zona Pública / DMZ"

            VMRouter[🖥️ VM Router <br>Debian<br>Public IP + Internal IP]

        end

  

        subgraph "Zona Privada (Bridge: vmbr1)"

            Note1[Red Interna: 10.0.0.0]

            VMOpenCTI[🐳 VM OpenCTI<br>Docker Stack]

            VMWazuh[🛡️ VM Wazuh<br>SIEM]

            VMOPNsense[🔥 VM OPNsense<br>Firewall]

        end

    end

  

    Termius -- "SSH Tunnel (Port 22)" --> NIC

    NIC --> VMRouter

    VMRouter -- "Forwarding 8006" --> ProxmoxHost

    VMRouter -- "Forwarding 8080/3000" --> VMOpenCTI

    VMRouter -- "Forwarding 443" --> VMWazuh

    VMRouter -- "Traffic Control" --> VMOPNsense

  

    style VMRouter fill:#f96,stroke:#333,stroke-width:2px

    style UserPC fill:#fff,stroke:#333

    style VMOpenCTI fill:#bbf,stroke:#333

    style VMWazuh fill:#bfb,stroke:#333
````
```mermaid
sequenceDiagram
    participant Local as PC Local (Browser/Termius)
    participant Bastion as VM Router (SSH Server)
    participant Target as Destino (Proxmox/OpenCTI)

    Note over Local, Target: Establecimiento del Túnel SSH
    Local->>Bastion: SSH Connect (User@Public_IP -L 8006:ProxmoxIP:8006)
    Bastion-->>Local: Conexión Aceptada (Canal Encriptado)
    
    Note over Local, Target: Acceso al Servicio Web
    Local->>Local: Browser request -> localhost:8006
    Local->>Bastion: Envío de paquete dentro del túnel
    Bastion->>Target: Reenvío a IP Privada:8006
    Target-->>Bastion: Respuesta HTTP
    Bastion-->>Local: Retorno por túnel
    Local->>Local: Renderizado en Navegador
````

